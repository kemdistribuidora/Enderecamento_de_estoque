# Sem estas vars, backend usa arquivo local (backend/data.sqlite) -- bom pra dev.
# Preencha as duas pra apontar pro banco Turso remoto (compartilhado entre maquinas).
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
